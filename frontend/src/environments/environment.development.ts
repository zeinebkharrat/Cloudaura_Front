export const environment = {
  production: false,
  /** Dev proxy → Flask (travel-recommendation/app.py). Empty = use static /assets/travel-match-model.json only. */
  travelRecommendationApiUrl: '/travel-rec',
};
