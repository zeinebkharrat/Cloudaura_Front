# Local profile overrides (optional)
# Start backend with profile local to use this file:
#   .\\mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=local

# Example local DB override:
# spring.datasource.url=jdbc:mysql://localhost:3306/Yallatn?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=

# Example local secrets:
# jwt.secret=replace-with-your-local-secret
# stripe.api.key=sk_test_xxx
